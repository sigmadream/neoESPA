# 2026-04-03 프로젝트 회고록

## 1. 개요
본 문서는 백엔드 아키텍처 리팩토링 및 학생 코드 이력 추적(Snapshot) 기능 구현에 대한 기술적 회고를 담고 있습니다.

## 2. 주요 작업 내역

### [백엔드] 아키텍처 리팩토링 및 최적화
- **모델 통합:** `AssignmentBase` 추상 클래스를 도입하여 `Homework`와 `Exam` 모델의 공통 필드를 통합 관리하고 중복 코드를 제거함.
- **권한 제어 단일화:** `require_staff` 의존성을 신설하여 라우터 내 반복되는 권한 체크 로직을 선언적으로 분리함.
- **상태 관리 개선:** `GradingService` 내에서 `Submission` 상태 업데이트 로직을 SSOT(Single Source of Truth) 원칙에 따라 정돈함.
- **마이그레이션 체계 도입:** `Alembic`을 프로젝트에 적용하여 프로덕션 환경에서의 안전한 스키마 변경 프로세스를 구축함.

### [백엔드] 코드 스냅샷 기능 (Storage Optimization)
- **스냅샷 엔진:** 학생의 중간 작업물을 저장하는 `CodeSnapshot` 모델 및 API 구현.
- **저장소 최적화:** `zlib` 압축 및 Base64 인코딩을 적용하여 텍스트 데이터의 공간 복잡도를 최적화함.
- **중복 방지:** 직전 스냅샷과 내용이 동일할 경우 저장을 생략하는 지능형 필터링 적용.

### [프론트엔드] 자동 저장 및 시각화 (User Experience)
- **자동 저장/복원:** `HomeworkSubmissionPanel`에 Debounce 기반 자동 저장 및 페이지 로드 시 최신본 복원 로직 구현.
- **UI 피드백:** 실시간 저장 상태(Saving, Saved, Error)를 시각화하여 사용자 불안감 해소.
- **어드민 뷰어:** `Monaco DiffEditor`를 도입하여 학생의 코드 변경 이력을 시간순으로 대조할 수 있는 고급 시각화 UI 구축.
- **성능 최적화:** `next/dynamic`을 사용하여 무거운 에디터 라이브러리를 지연 로딩(Lazy Loading) 처리함.

## 3. 기술적 이슈 및 해결
- **SQLModel 상속 충돌:** 부모 클래스의 `sa_column` 공유 문제로 인한 `ArgumentError` 발생. 각 테이블 모델에서 필드를 오버라이드하여 독립적인 컬럼 객체를 할당함으로써 해결함.
- **Docker/Local 경로 불일치:** Alembic 실행 시 SQLite 경로 인식 문제 발생. `env.py`에서 환경 변수에 따라 경로를 동적으로 변환하도록 로직 보강.

## 4. 향후 보완 과제 (Missing Parts)
- **API Rate Limiting:** 스냅샷 API에 대한 유저별 호출 횟수 제한 로직 추가 필요.
- **데이터 정리 정책 (Retention):** 오래된 스냅샷 데이터의 자동 삭제 또는 오카이빙 기능.
- **압축 레이어 공통화:** `Submission` 모델 등 시스템 전반의 대용량 텍스트 필드에 공통 압축 레이어 적용.
- **UI 성능 고도화:** 스냅샷 리스트 Pagination 또는 Virtual List 적용.

## 5. 결론
이번 리팩토링 및 기능 추가를 통해 시스템의 유지보수 효율과 교육적 가치를 동시에 높였습니다. 특히 데이터 압축과 지연 로딩을 통해 자원 효율성을 확보한 점이 고무적이며, 향후 Rate Limit 등 보안 강화 작업을 통해 프로덕션 안정성을 최종 완성할 예정입니다.
